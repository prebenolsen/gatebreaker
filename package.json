{
  "name": "gatebreaker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gatebreaker — an automatic action/resource progression game (prototype framework).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
