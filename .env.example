# Gatebreaker — environment variables
# Copy to ".env" and fill in. The user owns all external Supabase configuration.
# When these are absent, the game falls back to a local (localStorage) highscore store
# so the prototype always runs.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
